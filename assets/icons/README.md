# Iconos PWA (placeholders)

Estos SVG son placeholders temporales para la PWA.

Reemplazar por iconos definitivos:
- `icon-192.svg` (equivalente 192x192)
- `icon-512.svg` (equivalente 512x512)

También puedes usar PNG con los mismos nombres (actualizando `manifest.json` y `index.html`).
